# GitHub Copilot Repository Instructions

Before changing code, read `/AGENTS.md` and the nearest nested `AGENTS.md`. Those files are
canonical. Read referenced documents under `/docs` and do not invent unresolved campaign
rules.

Every behavioral change must include appropriate tests. Run the repository verification
commands before declaring completion. Preserve architecture, secrecy, audit, concurrency,
licensing, and intellectual-property constraints from `AGENTS.md`.
